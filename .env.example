# Optional: copy to `.env` in this folder (next to `package.json`, not `backend/.env`)
# Development:
REACT_APP_API_BASE_URL=http://127.0.0.1:5000

# Production build: see .env.production.example and deploy/DEPLOYMENT.md
