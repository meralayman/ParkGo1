# Copy to .env.production.local before `npm run build`
# CRA inlines REACT_APP_* at build time.

# After Certbot HTTPS is enabled, set your domain (no trailing slash):
REACT_APP_API_BASE_URL=https://your-domain.com

# Or leave this file empty / omit the variable — production uses the same origin as the page (http or https).
