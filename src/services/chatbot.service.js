/**
 * @deprecated Use `../api/chatApi` — persistent chat via GET /api/chat/history and POST /api/chat/message.
 */
export { fetchChatHistory, sendChatMessage, CHAT_MIN_SEND_INTERVAL_MS } from '../api/chatApi';
